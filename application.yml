 datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jrtp-ish-db
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
